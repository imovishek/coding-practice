#include<stdio.h>
#include<math.h>
typedef unsigned int uint;

uint dec_bin(uint dec);

void main()
{
 uint dec,bin,ara[100];
 int i;
 while(1){

 printf("Enter a decimal number(0 to exit):");
 scanf("%d",&dec);
bin=0;
 if(dec<1){break;}
 for(i=0;dec>0;i++)
     {
     ara[i]=dec%2;
     dec=dec/2;
     bin+=ara[i]*pow(10,i);
     }
     i--;
for(;i>=0;i--)
     {
printf("%d",ara[i]);
     }
     printf("\nAnother: %d\n",bin);


 }

}



